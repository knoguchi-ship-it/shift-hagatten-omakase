# シフトはがってんおまかせ！ 開発ルール

## 最優先

- このリポジトリの仕様上の正本は `docs/PRODUCT_SPEC.md`、実装規約の正本は `docs/ENGINEERING_RULES.md` とする。
- 未完了の計画項目がある間、部分実装・型チェック・ビルド成功を「完成」と表現しない。
- `dist/release/` には、テスト済みの配布インストーラだけを置く。中間生成物、展開版、ログ、秘密情報は置かない。
- 既存データを破壊しない。マスタ削除は論理削除と復元を基本とする。
- ユーザーが「継続」「完成まで」「一気に」と指示した場合、部分作業の報告、型チェック、ビルド成功を理由に作業を中断・完了扱いしない。次の未完了計画項目を直ちに実行する。
- 進行を止めてよいのは、完成条件を満たした場合、ユーザーの選択が必要な仕様判断、権限承認、安全上の確認が必要な破壊的操作だけである。
- ターン終了時は、未完了計画、直近の検証結果、次に再開すべき具体的作業を `docs/HANDOVER.md` へ更新する。

## セキュリティ

- APIキー、トークン、パスワード、個人情報、SQLite実データ、ログ、バックアップを出力・コミット・画面共有しない。
- Electronは `contextIsolation: true`、`nodeIntegration: false`、`sandbox: true` を維持する。RendererからNode APIへ直接アクセスさせない。
- ローカルHTMLには制限的なCSPを設定する。リモートコンテンツは原則読み込まず、必要時はHTTPSの許可先を固定する。
- IPCは許可したチャネルだけを公開し、Main側でIPC送信元、入力値・ファイルパス・操作対象を検証する。
- ナビゲーションと新規ウィンドウ作成を既定で拒否する。外部URLは信頼済みHTTPS URLだけを明示確認して開く。
- 外部URL、ファイル読込、復元、削除、Excel出力は最小権限・明示確認・安全な既定値を使う。復元ファイルはサイズ、SQLite整合性、想定スキーマを検証し、復元前バックアップを必須にする。
- 例外を握り潰さない。エラーは利用者向けの日本語メッセージと、秘密情報を含まないローカルログに分ける。
- リリース前に `npm audit --omit=dev`、IPC境界、依存関係、生成物の秘密情報混入を確認する。高・重大脆弱性は解消なしに出荷しない。中・低については影響、緩和策、修正予定をリリース記録に残す。
- Electron、Chromium、Node.js、主要依存関係を定期更新し、更新前後で回帰テストを行う。

## 実装品質

- DRY、単一責務、型安全を守る。制約判定はUI、生成、検証、Excel出力で重複実装しない。
- Domain/生成/検証はElectron・Reactに依存しないモジュールに分離し、ユニットテスト可能にする。
- UIは日本語業務アプリとして、操作状態、保存状態、影響範囲、エラー理由を明示する。
- キーボード操作とアクセシビリティを実装し、カスタムグリッドはARIA Gridのフォーカス規約に従う。
- 50名×31日を前提とし、表示は仮想化、計算はUIスレッドから分離する。

## 検証・配布

- 各実装単位で型チェックと該当テストを実行する。
- 完成前に、ユニット、E2E回帰、テスト観点表による独立評価、セキュリティ確認、Windowsインストーラの実機起動確認を行う。
- 失敗したビルドや検証結果を隠さず、失敗状態のインストーラを配布しない。
