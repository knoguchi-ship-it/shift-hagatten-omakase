# 引継ぎ — 2026-08-28

## 最新更新（2026-08-28）

- P1の未消化だったSQLiteスキーマ移行、勤務種別の開始/終了時刻、勤務日カウント、雇用区分、職種表示順、NGペア/翌日ルール削除を実装した。既存SQLiteは外部キー/CHECK付きのスキーマバージョン2へトランザクション移行し、参照不整合があれば置換前にロールバックする。旧DBの勤務履歴を保持する回帰テストを追加した。
- 設定画面で新規職員・勤務種別と、それらを参照するルールを一度に保存できる。仮IDは単一トランザクション内で実IDへ解決する。職員削除も影響表示・明示確認・論理削除/復元を行う。
- 勤務種別時刻はマスタからExcelの勤務種別見出しまで出力する。グリッドには行・列の文脈とキーボード操作説明を追加したが、完全なARIA Grid構造およびスクリーンリーダー評価は未完了である。
- 直近の検証は `npm run typecheck`、`npm test`（6ファイル・28件）、`npm run build` が成功。ViteのCJS設定に関する将来互換性警告は継続。`npm audit --omit=dev` はhigh/critical 0件、`exceljs`経由の`uuid`にmoderate 2件を検出し、破壊的な自動修正は未適用。
- `npm run test:e2e` は未解消のGPUプロセスDLL初期化失敗により、このホストではRendererを起動できない。インストーラ実機起動、50名×31日操作、スクリーンリーダー、独立評価も未実施。従って出荷・完成判定は不可。
- 配布状態を再確認した。外部デプロイ先、CI/CD設定、Gitリモート/履歴はこの作業ツリーで確認できない。`dist/release/`の唯一のインストーラは2026-08-02作成のv0.1.0（未署名）で、現行ソースのビルド・E2E・実機起動を経た成果物ではない。デプロイ済み・出荷済みとして扱わない。
- GitHub公開を実施した。`https://github.com/knoguchi-ship-it/shift-hagatten-omakase` を公開リポジトリとして作成し、ローカルGitを初期化して初回コミットを`main`へpushした。`.gitignore`でログ、SQLite、生成物、過去のブラウザ出力を除外済み。GitHub公開はソース管理の開始であり、未検証の配布物を出荷したことを意味しない。

### 次に再開する具体的作業

1. Electronが正常起動するWindows環境で、E2Eの主要フロー、匿名50名×31日、キーボードのみ、NVDA等の支援技術、`docs/ACCEPTANCE_TEST_MATRIX.md`の全観点を実施する。
2. GridのARIA `row`/`rowheader`/`gridcell`構造とフォームのエラー関連付けを、実際の支援技術で検証して必要なら修正する。
3. 正規Gitクローンを復旧して基準差分を確定後、セキュリティ再監査、Windowsインストーラの実機検証、配布判定を行う。未検証のインストーラを`dist/release/`へ置かない。

このドキュメントは、次にこのプロジェクトを引き継ぐ担当者（人間・AIエージェント問わず）が状況を把握し、迷わず作業を再開できるようにするための最終版サマリである。過去の経緯は本文中に要約し、詳細は該当コミット時点のファイルを参照すること。

## キャッチアップ記録（2026-08-28）

- 現行ファイルの更新日時を確認した。ソース本体の最終更新は2026-08-15、匿名50名×31日フィクスチャと受入観点表の最終更新は2026-08-21であり、その後の実装変更は見当たらない。
- 今回の再確認では`npm run typecheck`が成功、`npm test`は4ファイル・19件すべて成功した。Vitest実行時には、`vitest.config.ts`をCommonJSとして読み込むことに関するViteの将来互換性警告が出るが、テスト失敗はない。
- E2E、実機受入、セキュリティ監査、インストーラ実機確認は今回未実施。2026-08-21のElectronレンダラ起動クラッシュ（GPUプロセス初期化時の外部DLL失敗）の記録を有効な未解決環境制約として扱う。
- `.git`ディレクトリは空で、`git status`および履歴確認はできない。現時点では差分・コミットの追跡が不能な作業ツリーである。開発を進める前に、正規のGitクローンまたはGitメタデータを復旧し、意図しない差分がないことを確認する。
- `dist/release/`には2026-08-02作成の`シフトはがってんおまかせ！ Setup 0.1.0.exe`が残るが、現行ソースに対応する実機検証済み配布物とは確認できない。配布対象にせず、次回の`package:win`と実機検証が完了するまで保留する。
- 静的自己監査を実施し、グローバル標準への適合・業務利用可とは判定しなかった。詳細と是正順は`docs/SELF_AUDIT_2026-08-28.md`を参照する。最優先は、未保存変更の上書き防止、`file://`時の実効CSP、削除済み勤務種別の履歴表示である。
- 上記P0と保存整合性の一部を実装した。`shiftc://app`カスタムプロトコル、既定拒否の権限ハンドラ、未保存保護、削除済み勤務種別の当月履歴表示、条件/設定の原子保存、Atomicsによる生成中止、既存月を開く導線を追加した。型検査・ビルド・単体25件は成功した。
- `npm run test:e2e`は、本ホストのGPUプロセスDLL初期化失敗によりRendererが起動できず失敗した。テスト引数のGPU無効化でも解消しなかった。隔離ユーザーデータでの直接起動でも同じGPUプロセス失敗を再現したため、現時点では製品ロジックのE2E成否を判定できない。
- `npm audit --omit=dev`はhigh/critical 0件、`exceljs`経由の`uuid`に関するmoderate 2件を継続検出する。提示される自動修正は破壊的な`exceljs`ダウングレードのため未適用。

### 次に再開する具体的作業

1. 正規のGit履歴を復旧または正規クローンへ移行し、作業ツリーの基準コミットを確定する。
2. 既存SQLiteを破壊しない外部キー/CHECK付きスキーマ移行を設計・実装し、勤務種別の開始/終了時刻、勤務日カウント、雇用区分、職種表示順、ルール削除を仕様どおり実装する。
3. Gridの完全なARIA行/列ヘッダ、フォームの永続ラベル/エラー関連付け、色コントラスト、スクリーンリーダー評価を完了する。
4. Electronが正常起動するWindows環境で、匿名シナリオを用いてE2Eと受入観点A-01〜A-11を実施し、独立評価とインストーラ検証を行う。

## 状態（結論）

**未完成。配布・完成判定は禁止。** 主要機能は実装済みで、2026-08-28に型チェックと単体テストは再成功を確認した。一方、2026-08-21のE2EはElectronレンダラの起動クラッシュにより失敗しており、現行ソースでのE2E再成功は未確認である。加えて以下の理由により、受入基準（`docs/PRODUCT_SPEC.md` §7）を満たしたとは断定できない。

- 実機Windows環境でのインストーラ起動確認が未実施
- 50名×31日規模での手動操作性確認が未実施
- 直近の不具合修正に対する第三者評価の再実施が未実施
- 正規のGit履歴がこの作業ツリーに存在せず、基準コミットと差分が確認不能

次の担当者は、まず「未完了タスク」の1〜3を最優先で行うこと。

## プロジェクト概要

「シフトはがってんおまかせ！」— 介護施設向け月次勤務表作成Electronデスクトップアプリ。React（レンダラー）+ better-sqlite3（メインプロセスのローカルDB）+ exceljs（帳票出力）。ネットワーク接続を前提としない。

- 仕様の正本: `docs/PRODUCT_SPEC.md`
- 実装規約の正本: `docs/ENGINEERING_RULES.md`
- 開発ルール: `AGENTS.md`（セキュリティ・完了制御・継続実行ルールなど。**作業前に必ず読むこと**）

## これまでの経緯（要約）

1. **2026-08-03時点**: 職種テーブル・論理削除などの基礎部分のみ実装。未完成。
2. **2026-08-15（実装セッション）**: HANDOVERに残っていた7つの未完了項目（共有型基盤、職員⇔職種接続、職種別必要人数、勤務不可条件、生成エンジンのWorker分離、シフトグリッドの仮想化・キーボード操作、バックアップ復元、Excel帳票、IPCセキュリティ、テスト整備）をすべて実装。
3. **同日、第三者評価**: 実装に関与していない独立エージェントによるレビューで、受入基準未達の重大な不具合を2件（C-1: 翌日制約が自動生成で機能しない、C-2: 勤務種別マスタに削除・復元機能がない）と、高優先度2件（H-1: 復元前バックアップの不完全性、H-2: E2Eカバレッジ不足）、中優先度2件を発見。
4. **同日、修正**: Web検索で2026年時点のベストプラクティス（SQLite WAL/Backup API、Electron IPCセキュリティ、ARIA Grid）を確認したうえで、C-1・C-2・H-1・H-2・M-3・M-4を修正。
5. **同日、E2E回帰テスト**: 修正後に typecheck / build / vitest / Playwright E2E を再実行し回帰なしを確認。C-2向けの新規E2Eケースを追加。

## 実装済み機能

- 職種・職員・勤務種別マスタ（追加・編集・論理削除・復元）
- 職員マスタの職種紐付け（`role_id`）とExcel/NGペア選択への反映
- 日付×勤務種別×職種の3次元必要人数設定（`role_requirements`）
- 勤務不可曜日・勤務不可種別（`staff_unavailable_conditions`）
- 希望休、NGペア、翌日制約（夜勤入り→夜勤明け等、`countsAsWork=0`の種別への連鎖割当を含む）、月間上限日数、連勤上限、月間下限日数（`MIN_DAYS`違反として検出）を考慮した自動生成
- 生成は`worker_threads`で分離実行。進捗通知・中止・180秒タイムアウト（時間切れ時は現時点案+未充足一覧を返す）
- セル編集後500ms debounceでの違反再検証
- `react-window`による仮想化シフトグリッド。矢印キー移動・Enter編集・Esc取消・Ctrl+C/Vコピペ・違反一覧からのセルジャンプ・ARIA Grid属性
- SQLiteバックアップ（Backup API）／復元（サイズ上限・整合性チェック・スキーマ検証・復元前自動バックアップ・WALサイドカー処理）
- 30日ローテーションのローカルログ（`src/main/logger.ts`）
- Excel帳票出力（既存様式踏襲、職種名は`roles`テーブルJOIN）
- IPC送信元検証（`assertTrustedSender`）、zodによるペイロード検証、CSPの本番/開発分離
- テスト: vitest単体テスト18件、Playwright E2E 2件

## 検証コマンドと直近の結果（2026-08-15時点、いずれも成功）

```
npm run typecheck   # tsc（main/renderer 両方）
npm run build       # tsc + vite build
npm test            # vitest run（18件）
npm run test:e2e    # playwright test（2件、事前に npm run build が必要）
npm audit --omit=dev  # uuid(moderate)のみ残存、実害なしと判断（下記参照）
```

E2Eテスト（`e2e/main-flow.spec.ts`）は`SHIFTC_USER_DATA_DIR`環境変数でuserDataディレクトリを隔離するため、実DBを汚染しない。

## 未完了タスク（優先順）

### 再開プラン（2026-08-21）

以下の順で実行する。完了判定は第5段階の全項目が成功するまで行わない。

1. **ベースラインと受入準備**: 現行ソースで `typecheck`、build、unit、E2E、依存関係監査を再実行して結果を記録する。あわせて、50名×31日・複数職種・希望休・NGペア・勤務不可条件・翌日制約を含む匿名ダミーデータ、および受入観点表を整備する。
2. **仕様ギャップの実装**: 勤務種別マスタの開始・終了時刻（PRODUCT_SPEC §3.3）を、共有型・SQLiteスキーマ/移行・IPC検証・UI・Excel出力・テストまで一貫して実装する。加えて、連勤上限を生成時に積極的に回避するソフト制約を導入し、既存の事後違反検出だけに依存しない。
3. **規模・操作・アクセシビリティ検証**: 50名×31日で、仮想グリッド、キーボードのみの編集、違反セル遷移、生成の進捗/中止/180秒タイムアウト、スクリーンリーダーを実機確認する。問題があれば修正し、該当する自動テストを追加する。
4. **独立評価と回帰/セキュリティ確認**: 主要フロー（マスタ→条件→生成→編集→Excel→バックアップ/復元）のE2Eを充実させ、実装に関与していない評価者による観点表ベースの再評価を受ける。C-1/C-2/H-1/H-2/M-3/M-4の修正を実データ相当のシナリオで再確認し、IPC境界・依存関係・生成物の秘密情報混入も再監査する。
5. **配布物検証**: `npm run package:win`でNSISインストーラを作成し、クリーンなWindows環境でインストール、起動、主要フロー、アンインストールを実機確認する。成功したインストーラだけを`dist/release/`へ配置し、検証記録を残す。

### 直近の検証結果（2026-08-21）

- `npm run typecheck` は成功した。
- `npm test` は19件すべて成功した。ViteのCommonJS設定に関する将来互換性警告が残る。
- `npm run build` は成功した。
- `npm run test:e2e` は2件ともElectronレンダラの起動クラッシュで失敗した。本環境ではGPUプロセス初期化に必要な外部DLLを起動できず、製品コードを変えずにE2Eを通すことはできない。Electronが正常起動するWindows環境で再実行する。
- `npm audit --omit=dev` は`exceljs`経由の`uuid`に起因するmoderate 2件を報告した。提案される`--force`修正は破壊的な`exceljs`ダウングレードを伴うため未適用。
- 匿名50名×31日フィクスチャ（`src/shared/fixtures/acceptance-50x31.ts`）と受入観点表（`docs/ACCEPTANCE_TEST_MATRIX.md`）を追加した。
- 次回の具体的な再開作業は、Electronが正常起動するWindows環境で匿名シナリオを使いE2EとA-01〜A-08を実行すること。並行して第2段階の勤務種別開始・終了時刻の設計を開始できる。

1. **実機インストーラ確認**: `npm run package:win`でNSISインストーラをビルドし、実際にWindows環境へインストール・起動・主要フロー操作を行う。AGENTS.mdが出荷前必須と規定している。
2. **50名×31日規模の手動操作確認**（PRODUCT_SPEC §7）: ダミーデータを投入し、グリッドの操作性・キーボードのみでの編集・自動生成の180秒タイムアウト挙動を実測する。
3. **第三者評価の再実施**: 2026-08-15の修正（C-1/C-2/H-1/H-2/M-3/M-4）が実際に受入基準を満たしているか、実装に関与していない評価者に再度確認させる。特にC-1（翌日制約）は複雑なロジック変更のため、単体テスト以外の観点（実データでの生成結果）でも確認すると良い。
4. **アクセシビリティ実機確認**: ARIA Grid属性は付与済み（ベストプラクティスとの整合はWeb検索で確認済み）だが、スクリーンリーダー等の実機検証は未実施。
5. **連勤上限のソフト制約強化**: 現状は「累計勤務日数が少ない職員を優先」という単純なヒューリスティックのみで、生成時に連勤を積極的に回避する仕組みがない（事後検出の`MAX_CONSECUTIVE`違反のみ）。

## 既知の技術的負債（低優先度）

- `staff.role`旧カラム（`role_id`導入前の名残、未使用だが残置。破壊的削除を避けるため放置中）
- IPCハンドラの実装パターンが`main.ts`内で統一されていない（`safeHandle`ヘルパーと手動`assertTrustedSender`呼び出しが混在。全箇所で送信元検証漏れがないことは確認済み）
- 勤務種別マスタの開始・終了時刻フィールド（PRODUCT_SPEC §3.3で言及）が未実装
- `npm audit --omit=dev`で`uuid`(moderate, GHSA-w5hq-g745-h8pq)が`exceljs`経由で検出される。`exceljs`は`uuid.v4()`を引数なしで呼ぶのみで脆弱性の対象（`buf`引数付き呼び出し）に該当しないため実害なしと判断。非破壊的な修正版がリリースされ次第、`npm audit fix`で更新する

## セキュリティ確認状況

- Electron: `contextIsolation: true` / `nodeIntegration: false` / `sandbox: true`、新規ウィンドウ・画面遷移を拒否（対応済み）
- IPC送信元検証（`event.senderFrame`比較）: 対応済み
- IPCペイロードのzod検証: 対応済み
- CSPの本番/開発分離（開発時のみVite Dev ServerのHMR接続を許可）: 対応済み
- バックアップ復元時のファイルサイズ上限・`PRAGMA integrity_check`・必須テーブル検証・復元前自動バックアップ: 対応済み
- ログは30日ローテーションで秘密情報・SQLite実データを含めない設計: 対応済み
- `npm audit --omit=dev`の残存項目: 上記「技術的負債」参照

## 主要ファイルの地図

```
src/shared/types.ts              共有型・日付ヘルパー（Node/Electron非依存）
src/main/storage.ts              SQLiteスキーマ・CRUD・backup/restore
src/main/generation/engine.ts    生成エンジン（DB非依存の純粋関数、generate/validateCells）
src/main/generation/worker.ts    worker_threadsエントリ
src/main/main.ts                 Electronメイン・IPC・メニュー・CSP
src/main/preload.ts              contextBridge API
src/main/ipc-schemas.ts          IPCペイロードのzodスキーマ
src/main/logger.ts               30日ローテーションログ
src/main/workbook.ts             Excel帳票出力（exceljs）
src/renderer/src.tsx             React UI（単一ファイル、1200行超）
e2e/main-flow.spec.ts            Playwright E2E
src/main/*.test.ts, src/shared/*.test.ts, src/main/generation/*.test.ts  vitest単体テスト
```

## 次の担当者へのひとこと

型チェック・ビルド・自動テストが通っていることと、実際に受入基準を満たしていることは別物である（AGENTS.mdの完了制御ルールが繰り返し強調している通り）。特にC-1のように「テストは通るが実運用のシナリオでは機能しない」バグが過去に第三者評価で見つかった経緯があるため、次の一歩は必ず「未完了タスク」の1〜3（実機確認・規模確認・第三者再評価）から始めること。
